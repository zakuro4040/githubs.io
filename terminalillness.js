$(function() {
    $(".mainvisual-logo").css("display","none");
    $(".mainvisual-logo").fadeIn(1500);
    
    /*header-nav*/
    $("#about").click(function(){
        var about=$(this).attr(href);
        var position=$(id).offset().about;
    })

    $("#event").click(function(){
        var about=$(this).attr(href);
        var position=$(id).offset().about;
    })

    $("#member").click(function(){
        var about=$(this).attr(href);
        var position=$(id).offset().about;
    })

    $("#coback").click(function(){
        var about=$(this).attr(href);
        var position=$(id).offset().about;
    })

  });
  
